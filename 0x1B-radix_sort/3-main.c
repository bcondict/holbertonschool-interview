#include "sort.h"

/**
 * main - Entry point
 *
 * Return: Always 0
 */
int main(void)
{
	int array[] = {543, 2, 9, 15};
	size_t n = sizeof(array) / sizeof(array[0]);

	result = radix_sort(array, n);
	if 
	print_array(array, n);
	return (0);
}
